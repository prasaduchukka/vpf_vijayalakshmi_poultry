// Change this if the backend is hosted at a different URL than the frontend.
// Leave as '' if frontend and backend are served from the SAME origin (recommended for production).
window.API_BASE = '';
