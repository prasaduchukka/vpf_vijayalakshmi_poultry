server:
  port: ${PORT:8088}
  servlet:
    session:
      cookie:
        # On Render (HTTPS), set COOKIE_SECURE=true via environment variables.
        # Leave both at their defaults for local HTTP development.
        secure: ${COOKIE_SECURE:false}
        same-site: ${COOKIE_SAME_SITE:Lax}

spring:
  datasource:
    url: jdbc:postgresql://dpg-da7f372jnfac738klvng-a.oregon-postgres.render.com:5432/vpf_db_d6di
    username: ${DB_USERNAME:vpf_db_d6di_user}
    password: ${DB_PASSWORD:pl2yl9nz2j6Ah9oH744hiY8xZubRK9Zc}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Kolkata
  jackson:
    time-zone: Asia/Kolkata

# Default admin account created automatically on first run if no users exist.
# CHANGE THE PASSWORD after first login (Admin > Change Password), or set env vars below.
app:
  default-admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:https://vijayalakshmi-poultry-frontend.onrender.com,http://localhost:5500,http://127.0.0.1:5500,http://localhost:8088}